<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IntelliHouse2000App.Views.ClimateGraphsPage"
<<<<<<< HEAD
             xmlns:views="clr-namespace:IntelliHouse2000App.Views">
    <TabBar>
        <Tab Title="Logs">
            <ShellContent 
                Title="Kitchen"
                ContentTemplate="{DataTemplate views:KitchenGraphsPage}" />
            <ShellContent 
                Title="Bedroom"
                ContentTemplate="{DataTemplate views:BedroomGraphsPage}" />
            <ShellContent 
                Title="Livingroom"
                ContentTemplate="{DataTemplate views:LivingroomGraphsPage}" />
        </Tab>
    </TabBar>
</Shell>
=======
             xmlns:chart="clr-namespace:Syncfusion.Maui.Charts;assembly=Syncfusion.Maui.Charts"
             xmlns:climate="clr-namespace:IntelliHouse2000App.ViewModels"
             x:DataType="climate:ClimateGraphsPageViewModel">

    <!-- <ContentView> -->
    <!--     <VerticalStackLayout -->
    <!--         Spacing="25" -->
    <!--         Padding="30,0" -->
    <!--         VerticalOptions="Center"> -->
    <!-- -->
    <!--         <Label Text="{Binding KitchenValues.Count }" /> -->
    <!--         <Label Text="{Binding BedroomValues.Count}" /> -->
    <!--         <Label Text="{Binding LivingroomValues.Count}" /> -->
    <!--          -->
    <!-- -->
    <!--     </VerticalStackLayout> -->
    <!-- </ContentView> -->
    
    <chart:SfCartesianChart>
        <chart:SfCartesianChart.XAxes>
            <chart:CategoryAxis />
        </chart:SfCartesianChart.XAxes>

        <chart:SfCartesianChart.YAxes>
            <chart:NumericalAxis />
        </chart:SfCartesianChart.YAxes>  

        <chart:LineSeries ItemsSource="{Binding KitchenValues}"
                          XBindingPath="Timestamp" 
                          YBindingPath="Temperature"/>
        <chart:LineSeries ItemsSource="{Binding KitchenValues}" 
                          XBindingPath="Timestamp"
                          YBindingPath="Humidity"/>
    </chart:SfCartesianChart>

</ContentPage>
>>>>>>> develop
